## 1. Модель состояния и конфигурация

- [ ] 1.1 Добавить в модель статусов этапа состояние `retrying`, поле `next_retry_at` и миграцию PostgreSQL для существующих прогонов.
- [ ] 1.2 Явно задокументировать в настройках приложения и `.env.example`, что `CELERY_TASK_MAX_RETRIES` означает число дополнительных попыток, а задержка, максимум backoff и лимиты времени заданы в секундах.
- [ ] 1.3 Добавить в схемы API истории прогона состояние повтора, допустимое общее число попыток и время следующего запуска без нарушения совместимости существующих ответов.

## 2. Повтор задач конвейера

- [ ] 2.1 Ввести типы временных ошибок конвейера и определить адаптацию известных сетевых/внешних ошибок и `SoftTimeLimitExceeded` к этим типам.
- [ ] 2.2 Перевести обёртки задач BP-1--BP-7 на bound-задачи Celery с ограниченным экспоненциальным `self.retry` для временных ошибок.
- [ ] 2.3 Обновлять запись этапа атомарно перед планированием повтора, при его начале, успехе и окончательном исчерпании лимита; сохранять последнюю причину и время следующего запуска.
- [ ] 2.4 Защитить обработку повторно доставленной задачи блокировкой записи этапа и возвратом сохранённого результата для уже завершённого этапа.
- [ ] 2.5 Сохранить активность прогона во время `retrying` и убедиться, что финализация цепочки и допуск следующего планового прогона происходят только после окончательного результата текущего.

## 3. Отображение и эксплуатация

- [ ] 3.1 Дополнить историю запуска в административном API и фронтенде: отображать ожидание повтора, попытки, последнюю ошибку и плановое время следующей попытки.
- [ ] 3.2 Проверить обработку устаревшего прогона после жёсткого таймаута или потери worker'а: он должен завершаться наблюдаемым конечным статусом и освобождать слот.

## 4. Проверка

- [ ] 4.1 Написать модульные тесты классификации ошибок и расчёта задержек, включая предел backoff и трактовку максимума дополнительных попыток.
- [ ] 4.2 Написать интеграционные тесты успешного повтора, исчерпания лимита, неповторяемой ошибки, сохранения активного слота и недопуска параллельного планового прогона.
- [ ] 4.3 Проверить API/интерфейс истории для состояний `retrying` и окончательной ошибки, затем выполнить релевантные тесты проекта и `openspec validate --strict`.
