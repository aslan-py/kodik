"""Внутренние модули LLM-слоя (BP-1 Adaptive).

Пакет назван ``_llm`` (с подчёркиванием), чтобы не конфликтовать с
модулем ``processing/llm.py``, который остаётся публичным фасадом.

- ``constants`` — константы и параметры по умолчанию.
- ``json_utils`` — устойчивый разбор JSON-ответов LLM.
- ``prompts`` — шаблоны промптов.
- ``prompt_builders`` — сборка финальных промптов.
- ``schemas`` — Pydantic-модели ответов LLM.
- ``client`` — базовый LLM-клиент с ретраями/таймаутами.
- ``mappers`` — мапперы ответов LLM в доменные схемы.
- ``heuristics`` — эвристические fallback-реализации.
- ``chunking`` — оркестрация параллельного чанкированного анализа.
"""
