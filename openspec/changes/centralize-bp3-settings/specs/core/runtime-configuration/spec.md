## MODIFIED Requirements

### Requirement: Эксплуатационные параметры настраиваются без правки кода
Параметры, которые меняются при эксплуатации под конкретное развёртывание или поведение внешних источников — объём и глубина сбора, таймауты, степень параллельности, время жизни кэшей, режим прогона сбора, — SHALL задаваться конфигурацией окружения. То же SHALL относиться к параметрам обращения к внешним платным сервисам: объём и глубина запроса, временное окно, параметры вызова языковой модели и адрес её API. Изменение такого параметра SHALL NOT требовать правки исходного кода или пересборки приложения.

#### Scenario: Изменение эксплуатационного параметра
- **WHEN** требуется изменить объём собираемого, таймаут обращения к источнику, степень параллельности или время жизни кэша
- **THEN** это достигается изменением конфигурации окружения и перезапуском приложения, без правки исходного кода

#### Scenario: Параметр не задан в конфигурации
- **WHEN** приложение запускается, а эксплуатационный параметр в конфигурации не задан
- **THEN** применяется значение по умолчанию, и приложение запускается штатно — обязательными являются только доступы к внешним системам, но не параметры поведения

#### Scenario: Изменение стоимости прогона по внешним сервисам
- **WHEN** требуется изменить объём или глубину обращений к платному внешнему сервису, чтобы повлиять на стоимость и качество прогона
- **THEN** это достигается изменением конфигурации окружения, без правки исходного кода

#### Scenario: Смена провайдера внешнего API
- **WHEN** требуется переключить обращения на другой совместимый адрес API
- **THEN** это достигается изменением конфигурации окружения, а не правкой константы в исходном коде

## ADDED Requirements

### Requirement: Настройка с фиксированным набором значений проверяется при старте
Если настройка допускает только значения из заранее известного набора, приложение SHALL проверять её при старте и SHALL NOT передавать непроверенное значение во внешний сервис.

#### Scenario: Задано недопустимое значение
- **WHEN** приложение запускается, а настройке с фиксированным набором значений задано значение вне этого набора
- **THEN** запуск завершается ошибкой с указанием переменной и допустимых значений — до любого обращения к внешнему сервису

#### Scenario: Задано допустимое значение
- **WHEN** приложение запускается со значением из допустимого набора
- **THEN** запуск проходит штатно, и это значение используется при обращении к внешнему сервису
