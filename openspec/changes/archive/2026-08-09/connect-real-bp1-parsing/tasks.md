## 1. Точка входа реального сбора

- [x] 1.1 Создать `src/bp1/pipeline.py` с `run_bp1()` — по образцу докстрингов `src/bp2/pipeline.py`…`src/bp7/pipeline.py` (что делает этап, порядок шагов, соседние модули)
- [x] 1.2 `run_bp1()` открывает сессию (`AsyncSessionLocal`) и Redis (`redis_client.get_client()`), закрывает Redis в `finally` — по образцу `src/bp1/runner.py::run_pipeline`
- [x] 1.3 `run_bp1()` вызывает `AdaptiveRunner.run_all(session, redis_client)` с дефолтными параметрами класса (в настройки они переезжают отдельной задачей — см. design.md, Non-Goals) и коммитит сессию
- [x] 1.4 Свернуть список результатов по задачам в сводку прогона (`dict`) — сколько задач обработано, сколько снимков создано, сколько пропущено/без изменений/с ошибкой; формат согласовать с тем, что печатают остальные этапы

## 2. Настройка TRUE_PARSING

- [x] 2.1 `core/config.py` — поле `true_parsing: bool = False` в блок рядом с `true_alerting`, с комментарием про выбор реализации этапа 1
- [x] 2.2 `.env` и `.env.example` — переменная `TRUE_PARSING=False` с пояснением; значение по умолчанию сохраняет текущее поведение

## 3. Реестр и раннер

- [x] 3.1 `core/pipeline/registry.py::StageDescriptor` — поле под альтернативную реализацию-заглушку (по образцу `run_reparse`), по умолчанию `None`
- [x] 3.2 У этапа 1 основной `run` — `run_bp1`, альтернативный — текущая обёртка над `bp1_stub.clear`/`bp1_stub.seed`; `is_stub` этапа 1 становится `False` (основная реализация настоящая), `requires` — `SearchTask`
- [x] 3.3 Обновить докстринг `registry.py` — этап 1 больше не заглушка по умолчанию, у него две реализации
- [x] 3.4 `core/pipeline/runner.py::run_stage()` — параметр выбора реализации: `None` берёт значение из `settings.true_parsing`, явное значение переопределяет
- [x] 3.5 `run_stage()` проставляет `StageResult.is_stub` по фактически выполненной реализации, а не по полю дескриптора
- [x] 3.6 `_preflight()` пропускается для заглушки; для реального сбора этапа 1 проверяет наличие хотя бы одной активной `search_task` и даёт свой текст ошибки («заведите задачи сбора»), а не типовой «прогоните предыдущий этап»
- [x] 3.7 `run_all()` — тот же выбор реализации для этапа 1 (по настройке), сводка помечает фактическую реализацию
- [x] 3.8 `core/pipeline/cli.py` — аргумент разового переопределения режима этапа 1 (по образцу `reparse`)

## 4. Заглушка перестаёт сносить задачи сбора

- [x] 4.1 `core/scripts/stages/bp1_stub.py::clear()` — `clear_from(session, RawItem)` вместо `SearchTask`: сырьё и всё построенное на нём сносятся, конфигурация задач сбора остаётся
- [x] 4.2 `seed()` — «найти или создать» задачу сбора вместо безусловной вставки: на `search_task` висит `UniqueConstraint(competitor_id, source_id, trigger_id)` и partial unique index для `trigger_id IS NULL`, повторная вставка упала бы на конфликте
- [x] 4.3 Обновить докстринг модуля и `clear()` — что именно чистится и почему задачи сбора не трогаются
- [x] 4.4 Проверить, что блок `if __name__ == '__main__':` (самостоятельный запуск сидера) по-прежнему отрабатывает дважды подряд без ошибок — проверено: `search_task` id не меняется между прогонами, дублей нет, `raw_item` пересоздаётся

## 5. Админка

- [x] 5.1 `api/admin/pipeline_control.py` — у кнопки этапа 1 переключатель выбора реализации через `widget_action_props=WidgetActionProps(arguments=[...])` с `WidgetType.Switch`; `WidgetType.Checkbox` не использовать (в FastAdmin 0.10.0 не работает — см. tasks.md изменения `add-pipeline-reparse-and-admin-ui`, задача 4.7)
- [x] 5.2 Положение переключателя по умолчанию соответствует `settings.true_parsing`
- [x] 5.3 Значение из `payload` явно приводится к `bool` перед передачей в `run_stage` (тот же приём, что у `_reparse_requested`)
- [x] 5.4 Убедиться, что `_format_result` показывает пометку `[ЗАГЛУШКА]` по фактически выполненной реализации (следствие 3.5, отдельной правки не потребовалось — `StageResult.is_stub` уже приходит из `run_stage` фактическим)

## 6. Документация

- [x] 6.1 `core/scripts/SCRIPTS_README.md` — как запускается реальный сбор, чем отличается от `stages.bp1` (демо-датасет) и `stages.bp1_stub` (заглушка), и что переключает `TRUE_PARSING`
- [x] 6.2 `src/bp1/README.md` — раздел про точку входа `run_bp1()` и её место в конвейере (файл большой, дописать в подходящее место, не переписывать целиком)

## 7. Проверка

- [x] 7.1 `TRUE_PARSING=False`: прогнать этап 1 из CLI и кнопкой — отрабатывает заглушка, в сводке пометка заглушки, 6 новостей на месте
- [x] 7.2 Завести задачу сбора через админку, прогнать заглушку — убедиться, что задача НЕ удалилась, а снимок привязался к существующей задаче — проверено: вручную созданная задача (др. конкурент) пережила прогон заглушки
- [x] 7.3 Прогнать заглушку дважды подряд — второй запуск не падает на уникальном индексе `search_task` — проверено выше (раздел 4) и повторно здесь через CLI
- [x] 7.4 `TRUE_PARSING=True` без задач сбора — preflight даёт понятную ошибку про задачи сбора, а не про предыдущий этап — проверено: «нет данных в "search_task" — заведите хотя бы одну активную задачу сбора… в админке»
- [x] 7.5 `TRUE_PARSING=True` с заведённой задачей сбора — реальный прогон доходит до `raw_item`; зафиксировать, какой режим `AdaptiveRunner` отработал (см. design.md, Open Questions) — проверено на реальных данных (конкурент «ЯндексЕда», источник `ptsecurity.com`, заведены пользователем): `mode='adaptive'` (дефолт `run_bp1()`) выполнился, `raw_item` сохранён (`status=saved`, 5 элементов). LLM-ключ в окружении оказался невалиден (`401 Incorrect API key`) — адаптивный парсер деградировал до эвристического извлечения без LLM и всё равно сохранил результат; это конфигурационная проблема окружения (ключ), не связана с кодом этой задачи — точка входа и preflight отработали корректно
- [x] 7.6 Разовое переопределение: при `TRUE_PARSING=True` кнопкой запустить заглушку и наоборот — настройка не меняется, выполняется выбранное. Направление «заглушка→реал» подтверждено в 7.5 (default `False` + `true_parsing=True` → выполнен реальный сбор). Направление «реал→заглушка» подтверждено логикой `_resolve_use_stub` (юнит-тест выше) и трассировкой вызова: с `settings.true_parsing=True` (замокано в процессе) и явным `true_parsing=False` вызов дошёл до `bp1_stub.seed()` — маршрутизация верна; сам прогон не завершился успешно из-за отсутствия справочника `dictionaries` в текущей БД (пользователь подготовил её под реальный источник) — пересеивать справочники не стал, чтобы не снести подготовленные пользователем реальные `search_task`/`raw_item`
- [x] 7.7 `ruff check` по новым и изменённым файлам — чисто
