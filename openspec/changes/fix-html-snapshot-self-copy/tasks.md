## 1. Исправление

- [ ] 1.1 `src/bp1/storage.py::copy_html_file()` — в начале функции сравнить `Path(parser_file_path).resolve()` и `Path(dest_path).resolve()`; при совпадении вернуть `dest_path` сразу, не доходя до `shutil.copy2`
- [ ] 1.2 Убедиться, что при совпадении путей лог не засоряется предупреждениями о неудачных попытках копирования (нет обращения к retry-циклу вовсе)
- [ ] 1.3 Обновить докстринг `copy_html_file()` — упомянуть новую раннюю проверку и почему она нужна (см. design.md, Decisions)

## 2. Тест

- [ ] 2.1 Регрессионный тест: создать файл с контентом, вызвать `copy_html_file(path, dest_dir=path.parent)` (тот же файл) — содержимое не изменилось, возвращённый путь верный
- [ ] 2.2 Существующий сценарий (копирование из другой папки) — тест не сломан, поведение то же

## 3. Проверка

- [ ] 3.1 `TRUE_PARSING=True`, реальный сбор на источнике, где путь снимка адаптивного парсера совпадает с целевой папкой — `raw_item.html_file_path` указывает на файл с реальным содержимым (не 0 байт)
- [ ] 3.2 Прогнать классический RPA-сбор (`fedresurs_rpa`, если доступен для проверки) — копирование из временной папки по-прежнему работает
- [ ] 3.3 `ruff check` по изменённому файлу, прогон `pytest`
