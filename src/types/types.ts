export type IncidentItem = {
    id: string
    data: string,
    incident: string,
    source: string,
    category: string,
    type: string,
    region: string,
    object: string,
    priority: string,
    index: string,
    tonality: string,
    taskId?: string,
};