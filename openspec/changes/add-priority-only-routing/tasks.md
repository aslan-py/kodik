## 1. Модель и миграция

- [ ] 1.1 `src/bp5/models.py::RoutingRule` — тип события становится необязательным (`Mapped[int | None]`), обновить комментарий колонки: пусто = «любой тип события»
- [ ] 1.2 `RoutingRule.__table_args__` — заменить существующее ограничение уникальности на пару: обычное ограничение для правил с типом + частичный уникальный индекс для случая «тип не задан» (по образцу `uq_search_task_no_trigger` в `src/bp1/models.py`)
- [ ] 1.3 `src/bp5/models.py::Alert` — тип события становится необязательным, обновить комментарий колонки: пусто = сработало правило по приоритету, тип не распознан
- [ ] 1.4 Обновить докстринг модуля `src/bp5/models.py` — описание маршрутизации упоминает оба вида правил
- [ ] 1.5 Сгенерировать ревизию Alembic; проверить в ней: снятие NOT NULL с двух полей, замена ограничения уникальности, отсутствие лишних изменений от автогенерации
- [ ] 1.6 Применить миграцию, убедиться, что существующие правила и алерты не пострадали

## 2. Загрузка и группировка правил

- [ ] 2.1 `src/bp5/crud.py::load_routing_rules` — двухуровневая группировка: правила с типом по ключу «(тип, приоритет)», правила без типа по ключу «приоритет»
- [ ] 2.2 Обновить докстринг метода — что возвращается и почему две группы
- [ ] 2.3 Проверить, что справочники по-прежнему грузятся один раз на прогон, без запроса на каждое событие

## 3. Логика детектора

- [ ] 3.1 `src/bp5/pipeline.py::sync_alerts` — убрать ранний выход при нераспознанном типе: событие без типа должно доходить до проверки правил по приоритету
- [ ] 3.2 Собирать список подходящих правил в два прохода: сначала по «(тип, приоритет)» при распознанном типе, затем по «приоритету»
- [ ] 3.3 Схлопывать наложение до вставки: при совпадении получателя и канала оставлять правило с указанным типом (см. design.md, Decisions)
- [ ] 3.4 `build_alert_rows` — записывать распознанный тип, если он есть, даже когда сработало правило без типа; иначе оставлять незаполненным
- [ ] 3.5 Обновить счётчик `matched` в сводке прогона — сейчас он считает события с распознанным типом; определить и задокументировать, что он означает теперь
- [ ] 3.6 Обновить докстринг модуля `src/bp5/pipeline.py` — описание порога значимости и порядка шагов

## 4. API и админка

- [ ] 4.1 `api/schemas/routing_rule.py` — тип события становится необязательным в схемах чтения и записи
- [ ] 4.2 `api/admin/alerting.py` — поле типа в форме правила необязательно; подпись или подсказка явно говорит, что пусто означает «любой тип события»
- [ ] 4.3 Проверить отображение правила без типа в списке админки — пустая ячейка не должна читаться как незаполненное поле (см. design.md, Risks)

## 5. Документация и справочники

- [ ] 5.1 `src/bp5/BP5_README.md` — раздел про порог значимости описывает оба вида правил и отмечает, что правило без типа — самое широкое
- [ ] 5.2 Решить, добавлять ли пример правила без типа в `core/scripts/stages/dictionaries.py` и `simple_dictionaries.py`; по умолчанию не добавлять (см. design.md, Risks)

## 6. Проверка

- [ ] 6.1 Событие с распознанным типом и типовым правилом — алерт создаётся как раньше, тип в журнале заполнен
- [ ] 6.2 Событие БЕЗ распознанного типа при заведённом правиле по приоритету — алерт создаётся, тип в журнале пуст
- [ ] 6.3 Событие без распознанного типа и без правил по приоритету — алерта нет, событие помечено проверенным, повторный прогон его не поднимает
- [ ] 6.4 Наложение: заведены типовое правило и правило по приоритету с одним получателем и каналом — ровно одна доставка, в журнале запись с типом
- [ ] 6.5 Наложение с разными получателями — по алерту на каждого
- [ ] 6.6 Попытка завести два одинаковых правила без типа — отвергается ограничением уникальности
- [ ] 6.7 Повторный прогон BP-5 без изменений — новых алертов не создаётся
- [ ] 6.8 `ruff check` по изменённым файлам, прогон `pytest` (тесты BP-5 существуют — сверить с базовой линией)
