## 1. Восстановить тестовые моки BP1

- [ ] 1.1 Добавить `pytest-mock` в `requirements-dev.txt` и установить обновлённые dev-зависимости в используемое тестовое окружение.
- [ ] 1.2 Запустить `tests/bp1/fedresurs` и убедиться, что все 31 теста получают фикстуру `mocker` и не обращаются к реальному браузеру, сети или файловым внешним ресурсам.
- [ ] 1.3 Для проявившихся после запуска ошибок скорректировать моки на границах Playwright, ожиданий и извлечения данных, не подменяя тестируемую бизнес-логику.

## 2. Исправить применение лимита адаптивного сбора BP1

- [ ] 2.1 Изменить эвристический и селекторный пути парсера так, чтобы служебные и не-HTTP(S) ссылки фильтровались до применения `DEFAULT_MAX_NEWS`, с сохранением порядка полезных ссылок.
- [ ] 2.2 Добавить регрессионные тесты для шума в начале списка, недостаточного числа полезных ссылок и извлечения по селекторам.
- [ ] 2.3 Запустить целевые тесты адаптивного парсера и подтвердить выполнение delta spec.

## 3. Изолировать тесты BP5 от состояния рабочей БД

- [ ] 3.1 Добавить локальную autouse-фикстуру BP5, которая внутри общей тестовой транзакции временно отключает заранее существовавшие типы и правила и исключает ранее ожидавшие обработки события витрины, не выполняя `commit` или удаление.
- [ ] 3.2 Добавить проверку, что baseline не затрагивает данные, созданные фикстурами текущего теста, а после отката исходные строки и их активность сохраняются.
- [ ] 3.3 Запустить все 25 тестов BP5 на текущей заполненной БД и подтвердить отсутствие зависимости результатов от существующих правил маршрутизации.
- [ ] 3.4 Повторить тесты BP5 на чистой временной БД и удалить только созданную для проверки временную БД после успешного прогона.

## 4. Итоговая проверка

- [ ] 4.1 Запустить форматирование и статические проверки изменённых Python-файлов.
- [ ] 4.2 Запустить полный `pytest`, разобрать все ошибки, которые ранее скрывались за отсутствующей фикстурой `mocker`, и устранить относящиеся к этому change регрессии.
- [ ] 4.3 Зафиксировать итоговые количества пройденных, пропущенных и упавших тестов и подтвердить, что тесты не выполняли реальные сетевые отправки.
