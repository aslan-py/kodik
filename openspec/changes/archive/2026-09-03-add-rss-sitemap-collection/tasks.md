## 1. Зависимости и настройки

- [x] 1.1 Добавить `feedparser` в `requirements.txt` (по образцу
      существующих записей — версия + комментарий с назначением)
- [x] 1.2 Добавить в `core/config.py` (`Settings`) настройки по паттерну
      `BP1_*`: `bp1_feed_cache_ttl_seconds` (TTL записи обнаружения в
      `UnifiedCache`, длинный — по аналогии с кэшем адаптера),
      `bp1_feed_items_cache_ttl_seconds` (TTL кэша распарсенных
      материалов, короткий — порядка интервала прогона),
      `bp1_feed_fail_threshold` (порог самокоррекции кэша обнаружения,
      по умолчанию 2 — как `ADAPTER_FAIL_THRESHOLD`)
- [x] 1.3 Добавить те же переменные в `.env.example` с комментариями

## 2. Обнаружение фида

- [x] 2.1 Создать модуль обнаружения (конвенциональные пути: `/rss`,
      `/rss.xml`, `/feed`, `/feed.xml`, `/atom.xml`, `/sitemap.xml`,
      `/sitemap_index.xml`) — чистая функция, принимает базовый URL
      источника и (опционально) уже полученный HTML
- [x] 2.2 Разбор `<link rel="alternate" type="application/rss+xml"|
      "application/atom+xml">` из `<head>` переданного HTML (если он
      передан) — пробуется первым, до перебора конвенциональных путей
- [x] 2.3 Валидация найденного кандидата по факту разбора, не по одному
      HTTP 200 (пустой список записей/невалидный XML не считается
      успехом обнаружения)
- [x] 2.4 Тесты на обнаружение: самодекларация в `<link>` найдена; ни
      `<link>`, ни конвенциональные пути не отвечают (источник без фида);
      путь отвечает 200, но не является валидным RSS/Atom/sitemap

## 3. Извлечение материалов из RSS/Atom

- [x] 3.1 Сопоставление полей `feedparser`-записи в структуру результата
      сбора: `title`→title, `link`→url, `published`/`updated`→published_at,
      `summary`/`description`→text
- [x] 3.2 Обработка отсутствующих/нестандартных полей записи (не все
      фиды отдают все поля) — без падения на отдельной записи
- [x] 3.3 Тесты на сопоставление: полная запись; запись без описания;
      смешанный RSS 2.0/Atom в одном фиде на разных источниках (фикстуры)

## 4. `sitemap.xml` как источник URL-кандидатов

- [x] 4.1 Разбор `sitemap.xml` и `sitemap_index.xml` (вложенные sitemap)
      в список URL, с `<lastmod>`, если указан
- [x] 4.2 Передача URL-кандидатов в существующий deep-fetch статьи
      (переиспользование, без нового экстрактора)
- [x] 4.3 Применение существующего лимита `BP1_MAX_NEWS_PER_SOURCE` к
      списку кандидатов из sitemap; приоритет — кандидаты с более
      поздним `<lastmod>`, если он указан
- [x] 4.4 Тесты: плоский sitemap; sitemap_index с вложенными файлами;
      лимит применяется до deep-fetch, а не после

## 5. Кэш обнаружения и кэш материалов

- [x] 5.1 Кэш обнаружения в `UnifiedCache`: URL найденного фида/sitemap
      на источник (или отметка «фида нет»), TTL
      `bp1_feed_cache_ttl_seconds`
- [x] 5.2 Счётчик отказов кэша обнаружения по образцу
      `AdapterState.fail_count`: провал использования закэшированного
      фида увеличивает счётчик; успех обнуляет; по достижении
      `bp1_feed_fail_threshold` запись сбрасывается
- [x] 5.3 Кэш материалов в `UnifiedCache`: уже распарсенные записи фида
      на источник, TTL `bp1_feed_items_cache_ttl_seconds` (короткий,
      порядка интервала прогона) — заполняется первым успешным
      обращением к фиду в рамках прогона, читается последующими
      обращениями к тому же источнику
- [x] 5.4 Тесты: повторный `parse()` того же источника (другой
      конкурент) в пределах TTL кэша материалов не выполняет повторный
      HTTP-запрос к фиду; одиночный сбой не сбрасывает кэш обнаружения;
      два подряд сбоя сбрасывают

## 6. Встраивание в `AdaptiveParser.parse()`

- [x] 6.1 Ранний выход в начале `AdaptiveParser.parse()`: для
      `SourceType.NEWS` — попытка использовать фид (кэш материалов →
      кэш обнаружения → обнаружение с нуля) до вызова
      `_resolve_start_strategy`/`fetch_with_degradation`
- [x] 6.2 При успехе — сборка `AdaptiveParseResult`/`items` в форме,
      идентичной `_assemble_items` (`extra.news` с `ex_title`/`ex_url`/
      `ex_text`, `ex_method='rss'`/`'sitemap'`), возврат сразу,
      HTML-лестница для этого вызова не запускается
- [x] 6.3 При отсутствии/провале фида — `parse()` продолжается по
      существующему пути (`_resolve_start_strategy` →
      `fetch_with_degradation` → ...) без изменений, без увеличения
      счётчика отказов источника (circuit breaker)
- [x] 6.4 Тесты на встраивание: `SourceType.NEWS` с рабочим фидом не
      вызывает `fetch_with_degradation` вовсе; `SourceType.NEWS` без
      фида проходит существующий путь как раньше;
      `SourceType.REGISTRY`/`API`/`SPA`/`UNKNOWN` не затронуты
      (обнаружение фида не вызывается вовсе)

## 7. Разметка релевантности конкуренту (без LLM)

- [x] 7.1 Строковое сопоставление: нормализация (`lower`, без
      пунктуации, схлопнутые пробелы — тот же `norm()`, что у
      `dedup_key` в BP-2) имени конкурента и поиск вхождения в
      заголовке+тексте материала фида
- [x] 7.2 Материал без совпадения всё равно попадает в результат (без
      признака совпадения) — фильтрация не выполняется в BP-1
- [x] 7.3 Тесты: точное вхождение нормализованного имени находится;
      разный регистр/пунктуация не мешает совпадению; материал без
      совпадения сохраняется в результате без признака

## 8. Проверка сквозного сценария

- [x] 8.1 Полный прогон `tests/bp1/` — нулевая регрессия для источников
      без фида
- [x] 8.2 `ruff check`/`ruff format` на новых модулях
- [x] 8.3 Обновить `src/bp1/README.md`/`src/bp1/adaptive/README.md` —
      новый раздел про RSS/sitemap-путь сбора для новостных источников
