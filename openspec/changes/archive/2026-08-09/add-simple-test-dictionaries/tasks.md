## 1. Переписать `bp1_stub.py` на 6 новостей

- [x] 1.1 Заменить `STUB_NEWS`/`COMPETITOR_NAME`-константы на список из 6 новостей по таблице из design.md (домен, заголовок, текст — каждой новости своя реалистичная пара заголовок/текст, 2-3 предложения)
- [x] 1.2 Домены: `test1-alert-source.ru` (№1), `compromat-test.ru` (№2), `test3-source.ru` (№3), `shared-site-test.ru` (№4, №5, №6 — один и тот же)
- [x] 1.3 Текст новости №1 — однозначный сигнал категории «надзорная санкция и юридический риск» (упоминает «прокуратура», «проверка», «нарушения»)
- [x] 1.4 Текст новости №3 — содержит слово «промокод» (совпадает со стоп-словом из `simple_dictionaries`)
- [x] 1.5 `seed()` по-прежнему берёт ЛЮБОГО активного конкурента из справочника (не хардкодит имя) — эта часть уже правильная, не трогать; просто теперь на него вешаются 6 событий вместо 2
- [x] 1.6 Обновить докстринг модуля — 6 новостей вместо 2, кратко описать роль каждой (ссылка на design.md за подробностями не нужна — это отдельный файл, докстринг сам по себе должен быть понятен)

## 2. Создать `core/scripts/stages/simple_dictionaries.py`

- [x] 2.1 `seed(session)`: конкурент «ДатаСинтез» (один), источник `https://newssearch.yandex.ru` (переиспользовать `SOURCE_NAME` из `dictionaries.py`, не дублировать строкой)
- [x] 2.2 Чёрный домен: `compromat-test.ru`
- [x] 2.3 Стоп-слово: `phrase='промокод'`, `type=stop_word`
- [x] 2.4 Лимит антишума: `scope=media, max_count=1, window=run`
- [x] 2.5 Каналы: `email`, `telegram` (по образцу `CHANNELS` из `dictionaries.py`)
- [x] 2.6 Тип события: `keywords=['прокуратура']`, имя — по образцу существующего демо-типа BP-5 (`'судебный/надзорный риск'`)
- [x] 2.7 Тестовый пользователь-получатель: `full_name`, `email` и `telegram_id`-заглушки (числовой, не пересекается с реальными), `department_id=None` (поле nullable — резолвить отдел не нужно)
- [x] 2.8 Правило маршрутизации: 2 строки — (тип, П1) → (пользователь, email, instant) и (тип, П1) → (пользователь, telegram, instant)
- [x] 2.9 `clear(session)`: сначала `clear_from(session, PIPELINE_ORDER[-1])` (сносит весь пайплайн — FK иначе не даст удалить конкурента/источник), затем `delete()` по каждой из управляемых таблиц (competitor, source, black_domain, stop_word, topic_limit, channel, event_type, user, routing_rule)
- [x] 2.10 `if __name__ == '__main__':` блок — `run_stage('simple_dictionaries (тестовые справочники)', clear, seed)` по образцу остальных скриптов `stages/*.py`

## 3. Обновить `core/scripts/SCRIPTS_README.md`

- [x] 3.1 Добавить строку `python -m core.scripts.stages.bp1_stub` в таблицу «Поэтапные» (сейчас отсутствует)
- [x] 3.2 Добавить строку `python -m core.scripts.stages.bp7` в ту же таблицу (тоже отсутствует, не по вине этого change — чиним попутно)
- [x] 3.3 Добавить строку `python -m core.scripts.stages.simple_dictionaries` с кратким описанием (узкий набор справочников под 6-новостной тестовый сценарий bp1_stub)

## 4. Проверка вручную

- [x] 4.1 Прогнать `simple_dictionaries`, затем этап 1 (`bp1_stub`) — убедиться, что появился 1 `raw_item` с 6 событиями
- [x] 4.2 Прогнать этап 2 — убедиться: новость №2 `rejected(black_domain)`, №3 `rejected(stop_word)`, из №4/№5/№6 ровно одна `ok`, две `rejected(noise_limit)`; итого 2 строки `status=ok`
- [x] 4.3 Зафиксировать (по факту прогона 4.2), какая именно из №4/№5/№6 пережила антишум — обновить докстринг `bp1_stub.py`, если это стабильно воспроизводится
- [x] 4.4 Прогнать этап 3 — убедиться, что новость №1 получила приоритет П1 (если нет — см. design.md, Risks: поправить `RoutingRule.priority` вручную под фактическую категорию)
- [x] 4.5 Прогнать этап 4, затем этап 5 — убедиться, что алерт по новости №1 ушёл (реально либо в песочницу `TEST_EMAIL`/`TEST_TG`, либо по-настоящему — в зависимости от `TRUE_ALERTING`). Email — `sent`; telegram — `failed` из-за отсутствия сетевого доступа к `api.telegram.org` в этой рабочей среде (не связано с кодом/конфигурацией), маршрутизация и матчинг события отработали верно
