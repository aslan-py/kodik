## 1. Общая инструкция по заполнению справочников

- [x] 1.1 Добавить в `BP2_README.md` раздел перед `## Справочники` — короткое пояснение, зачем вообще заполнять `black_domain`/`stop_word`/`topic_limit` и в каком порядке они применяются к событию: сначала чёрный домен, затем стоп-слова (короткое замыкание — сработавший чёрный домен отменяет проверку стоп-слов), отдельно и позже — антишум `topic_limit` по уже записанным строкам
- [x] 1.2 Поправить вводную фразу `BP2_README.md:4` («Управляется через четыре справочника — их заполняет аналитик без изменения кода») — разделить: `region` предзаполнен на этапе разработки, три остальных (`black_domain`, `stop_word`, `topic_limit`) аналитик заполняет с нуля сам
- [x] 1.3 В разделе `### region — регионы` уточнить формулировку «Предзаполнен из `core/scripts/scripts_data/cities.json` при первом `seed_all`» — явно указать, что это делается на этапе разработки скриптом (не аналитиком вручную), потому что города/регионы почти не меняются; справочник уже готов к работе, редактировать нужно только если парсер прислал нераспознанное написание региона (эта часть уже есть в README — просто явно связать с «делается на этапе разработки»)
- [x] 1.4 В разделе `### black_domain — чёрный список доменов` добавить явное предложение: в отличие от `region`, этот справочник НЕ предзаполнен разработчиком — начинает пустым, домены заводит аналитик самостоятельно (так же как `stop_word`)

## 2. Сквозной пример: 5 событий, таблица после проверки чёрного домена

- [x] 2.1 Добавить упрощённую таблицу на 5 строк с колонками `№ | Конкурент | Домен | Заголовок | Описание | Причина отказа` (не сырые данные — заголовок из двух слов, описание из четырёх, только для наглядности):

  | № | Конкурент | Домен | Заголовок | Описание | Причина отказа |
  |---|-----------|-------|-----------|----------|-----------------|
  | 1 | КвантМайнд | example.ru | Итоги проверки | Компания опубликовала новый отчёт | — |
  | 2 | КвантМайнд | kompromat.ru | Скандал вокруг конкурента | Источник публикует компрометирующие материалы | `black_domain` |
  | 3 | КвантМайнд | news.ru | Реклама услуг | Компания предлагает скидку на продукт | — |
  | 4 | КвантМайнд | news.ru | Гороскоп недели | Ожидаются перемены в личной жизни | — |
  | 5 | Бегемот | news.ru | Бегемот в зоопарке | Городской зоопарк принял новых животных | — |

  (домен `kompromat.ru` должен реально быть в примере `black_domain` из раздела справочника выше по файлу — переиспользовать существующий пример `critics24.com` или явно связать со строкой 2)

## 3. Та же таблица после фильтра стоп-слов

- [x] 3.1 Показать ЭТУ ЖЕ таблицу (те же 5 строк) второй раз — с обновлённой колонкой «Причина отказа» после прогона стоп-слов/стоп-тем (строка 2 не меняется — чёрный домен уже остановил проверку; у строк 3 и 4 причина появляется впервые):

  | № | Конкурент | Домен | Заголовок | Описание | Причина отказа |
  |---|-----------|-------|-----------|----------|-----------------|
  | 1 | КвантМайнд | example.ru | Итоги проверки | Компания опубликовала новый отчёт | — |
  | 2 | КвантМайнд | kompromat.ru | Скандал вокруг конкурента | Источник публикует компрометирующие материалы | `black_domain` |
  | 3 | КвантМайнд | news.ru | Реклама услуг | Компания предлагает скидку на продукт | `stop_word` (`реклама`) |
  | 4 | КвантМайнд | news.ru | Гороскоп недели | Ожидаются перемены в личной жизни | `stop_topic` (`гороскоп`) |
  | 5 | Бегемот | news.ru | Бегемот в зоопарке | Городской зоопарк принял новых животных | `false_positive` (`бегемот в зоопарке`) |

- [x] 3.2 Короткой ремаркой пояснить под таблицей: строка 1 и после второго прохода осталась `ok` — уходит в BP-3 без отсева

## 4. Идемпотентность обычного прогона

- [x] 4.1 Добавить явное уточнение (рядом с примером или в общей инструкции из 1.1): в обычном прогоне (`reparse=False`) уже нормализованные снимки повторно не проверяются — новое/изменённое правило в справочнике подействует только на новые данные; чтобы пересчитать уже обработанные события новым правилом, нужен явный переразбор (`reparse=True`)

## 5. Финальная сверка

- [x] 5.1 Перечитать получившийся `BP2_README.md` целиком — проверить, что пример не противоречит реальному порядку фильтров в `src/bp2/pipeline.py` (`apply_filters`: сначала `is_black_domain`, потом `match_stop_word`) и реальным причинам `RejectReason` (`core/enums.py`)
