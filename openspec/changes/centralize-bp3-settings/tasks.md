## 1. Перечисление глубины поиска

- [ ] 1.1 Добавить в `core/enums.py` перечисление глубины поиска источников (`enum.StrEnum`, по образцу `LimitWindow`/`DeliveryMode`) со значениями внешнего сервиса и докстрингом
- [ ] 1.2 Убедиться, что значения перечисления совпадают с тем, что принимает API поиска

## 2. Настройки поиска источников

- [ ] 2.1 Добавить в блок BP-3 в `core/config.py` поля: число результатов на запрос (по умолчанию `2`), глубина поиска (типизирована перечислением из 1.1, по умолчанию базовая), временное окно (по умолчанию неделя)
- [ ] 2.2 `src/bp3/modules/source_finder_module.py` — заменить литералы `max_results`, `search_depth`, `time_range` на чтение из `settings`
- [ ] 2.3 `topic='news'` и параметры состава ответа оставить в коде — не настройки (см. design.md, Decisions)
- [ ] 2.4 Сверить «было/стало»: значения по умолчанию равны прежним литералам

## 3. Настройки вызова LLM

- [ ] 3.1 Добавить в `core/config.py` поля: адрес API, температура (по умолчанию `0`), лимит токенов (по умолчанию `4096`)
- [ ] 3.2 `src/bp3/config.py` — заменить константу `API_BASE` и литералы `temperature`/`max_tokens` на чтение из `settings`
- [ ] 3.3 Удалить закомментированную альтернативу адреса рядом с константой; упомянуть второй адрес в пояснении к переменной в `.env.example`
- [ ] 3.4 Убрать ставший ненужным `# noqa` на длинной строке докстринга `get_llm()`, если после правки строка укладывается в лимит
- [ ] 3.5 Сверить «было/стало»

## 4. Конфигурация

- [ ] 4.1 Добавить блок переменных BP-3 в `.env` и `.env.example` — в структуре, заданной `centralize-parsing-settings` (заголовок-комментарий, пояснение по каждой переменной)
- [ ] 4.2 В пояснениях явно отметить, что число результатов и глубина поиска влияют на стоимость прогона
- [ ] 4.3 Указать допустимые значения глубины поиска в пояснении к переменной

## 5. Проверка

- [ ] 5.1 Запуск с недопустимым значением глубины поиска — падает на старте с указанием переменной и допустимых значений, до обращения к внешнему сервису
- [ ] 5.2 Запуск без нового блока переменных в `.env` — стартует штатно на значениях по умолчанию
- [ ] 5.3 Прогнать BP-3 на подготовленных данных — результат совпадает с тем, что был до выноса параметров
- [ ] 5.4 Изменить число результатов поиска в `.env`, перезапустить, прогнать BP-3 — число найденных кандидатов в `source_candidate` изменилось соответственно
- [ ] 5.5 `ruff check` по изменённым файлам, прогон `pytest`
