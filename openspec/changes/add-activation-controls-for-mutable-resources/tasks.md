## 1. Обновить контракты запросов API

- [ ] 1.1 Добавить необязательный `is_active` в схему обновления каждого soft-delete ресурса: competitor, source, trigger, search task, black domain, stop word, topic limit, category, department, event type, channel и routing rule.
- [ ] 1.2 Обновить документацию PATCH/OpenAPI для общих справочников и поисковых задач: `is_active` деактивирует или реактивирует запись, а непереданные поля остаются без изменений.
- [ ] 1.3 Расширить административный payload `PATCH /users/{user_id}/role` необязательными `department_id` и `is_active`; переименовать элементы маршрута/сервиса и документацию так, чтобы они описывали частичное административное обновление пользователя.

## 2. Реализовать административное обновление пользователя

- [ ] 2.1 Изменить сервис пользователя и data-access слой: применять только переданные административные поля в одной транзакции и сохранить совместимость с обновлением только роли.
- [ ] 2.2 Валидировать переданный административный `department_id`, для отсутствующего отдела возвращать уже документированную ошибку not-found и сохранить текущие авторизацию и ограничения полей `/users/me`.

## 3. Дополнить справочник фильтров и диапазон дедлайнов

- [ ] 3.1 Добавить `showcase.media` в ответ `GET /filter-options`: получать уникальные непустые СМИ из витрины, сортировать без учёта регистра и документировать поле в OpenAPI.
- [ ] 3.2 Добавить необязательный фильтр `media` в `GET /showcase`, передать его через service/CRUD и применить как регистронезависимый поиск по подстроке.
- [ ] 3.3 Заменить `action_items.deadline` со списка дат на объект `{from, to}` с минимальной и максимальной видимой датой либо двумя `null`; сохранить ролевое ограничение viewer.
- [ ] 3.4 Заменить точный фильтр `GET /action-items?deadline` на включительные `deadline_from` и `deadline_to`, включая валидацию перевёрнутого диапазона и обновление OpenAPI.

## 4. Проверить контракты endpoint'ов

- [ ] 4.1 Расширить параметризованные API-тесты справочников: PATCH каждого soft-delete ресурса в inactive и обратно в active с проверкой статуса, схемы ответа и сохранности непереданного поля.
- [ ] 4.2 Добавить покрытие активации/реактивации PATCH для search task и убедиться, что существующий DELETE по-прежнему мягко деактивирует запись.
- [ ] 4.3 Добавить тесты административного пользователя: совместимость обновления только роли, обновление только отдела/активности и их комбинации, отсутствующий отдел, неавторизованные вызовы и отклонение `role`/`is_active` в `/users/me`.
- [ ] 4.4 Добавить контрактные тесты `filter-options` для `media`, дедлайн-диапазона с данными и без них, а также область видимости viewer.
- [ ] 4.5 Добавить тесты применимости `media` в `GET /showcase` и обеих границ `deadline_from`/`deadline_to` в `GET /action-items`, включая их комбинацию и перевёрнутый диапазон.
- [ ] 4.6 Обновить HTTP operation manifest/OpenAPI-assertions для изменённых схем, параметров и описаний операций.

## 5. Валидировать изменение

- [ ] 5.1 Запустить целевые API/service-тесты и полный набор тестов; устранить регрессии без выхода за документированную область.
- [ ] 5.2 Запустить используемые репозиторием lint/type-проверки и валидировать это изменение OpenSpec в строгом режиме.
