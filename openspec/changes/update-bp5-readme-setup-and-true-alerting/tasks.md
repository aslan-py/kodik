## 1. Верхнее описание и чек-лист заполнения

- [x] 1.1 В самом верху `BP5_README.md` (первый абзац, «BP-5 берёт новые/изменившиеся строки витрины...») явно уточнить: ключевые слова `event_type` ищутся ТОЛЬКО в заголовке события (`title`), не по всему тексту — сейчас это сказано только глубоко в файле, в разделе «Детекция и маршрутизация» (`title.lower()` против `keywords`)
- [x] 1.2 Добавить короткий чек-лист «что должно быть заполнено, чтобы алертинг сработал»: `event_type` (`is_active=true`, непустые `keywords` — иначе `detect_event_type` никогда не найдёт совпадение), `channel` (используются `email`/`telegram` — только под них есть реальная доставка, см. п.3), `user` (`email`, `telegram_id`, `is_active=true`), `routing_rule` (связывает тип+приоритет с конкретным пользователем и каналом). Явно указать: если любого из них не хватает — событие просто не даст алерта, БЕЗ ошибки (детектор либо не находит тип, либо не находит правило и молча идёт дальше, `src/bp5/pipeline.py::sync_alerts`)

## 2. Уточнения по конкретным справочникам

- [x] 2.1 В разделе `### event_type` добавить: заполняет аналитик; если справочник пуст или все правила `is_active=false` — уведомления сотрудникам не пойдут вообще, ни по одному событию
- [x] 2.2 В разделе `### channel` уточнить: реальная доставка сейчас реализована только для `email` и `telegram` (`src/bp5/pipeline.py`, шаг 5.5) — канал `dashboard` в коде не обрабатывается, строки `alert` с таким каналом останутся в `status=queued` навсегда (не ошибка, а особенность текущей версии)
- [x] 2.3 В разделе `### user` добавить: если у пользователя не заполнен `telegram_id`, доставка по каналу `telegram` для него невозможна (в модели `telegram_id` — `NOT NULL`, значит завести пользователя без него нельзя вовсе; но если для него нет активной строки `routing_rule` на канал `telegram` — алерт по этому каналу просто не создастся)
- [x] 2.4 В разделе `### user` описать, как узнать свой числовой `telegram_id` для тестовой отправки: написать `/start` боту `@userinfobot` в Telegram — он в ответ пришлёт числовой id аккаунта (тот же id, который нужен в `user.telegram_id`/`TEST_TG`)
- [x] 2.5 В разделе `### user` предупредить: демо-данные сидера (`core/scripts/stages/dictionaries.py`, константа `USERS`) используют ЗАВЕДОМО ФЕЙКОВЫЕ `telegram_id` (числа-заглушки вида `100000001`) — это осознанно, сидер работает с `deliver=False` и в сеть не стучится; но если включить `TRUE_ALERTING=true` на демо-данных как есть, попытка отправки в Telegram этим фейковым id гарантированно провалится (`status=failed`)

## 3. Исправление устаревшего раздела про доставку

- [x] 3.1 Переписать раздел «Доставка» — сейчас там написано «`instant` помечается `status=sent` сразу — это заглушка, реального обращения к Telegram Bot API / SMTP пока нет» — это уже НЕ так: `src/bp5/pipeline.py` шаг 5.5 реально вызывает `core.mail.send_email`/`core.telegram.send_telegram`, ловит исключение и ставит `sent`/`failed`. `digest` по-прежнему остаётся `queued` (джоба-сводка не реализована) — это единственное, что осталось верным из старого текста
- [x] 3.2 Убрать/переписать пункт «Реальная отправка в telegram/email» из раздела «Осознанно не реализовано» — заменить на актуальный список нереализованного: джоба-сводка для `digest`, Celery-обвязка, несколько типов на одно событие, доставка по каналу `dashboard` (см. 2.2)

## 4. Раздел про `TRUE_ALERTING`

- [x] 4.1 Добавить в `BP5_README.md` раздел, описывающий `TRUE_ALERTING` (`core/config.py::settings.true_alerting`, читается из `.env`): это переключатель ТОЛЬКО адресата, а не переключатель самой отправки —
  `false` → письмо/сообщение уходит на `TEST_EMAIL`/`TEST_TG` из `.env`; `true` → на реальные `email`/`telegram_id` получателя из `routing_rule`. Показать оба варианта явно (как в `.env.example`)
- [x] 4.2 Явно предупредить: попытка реальной отправки (SMTP/Telegram Bot API) происходит ВСЕГДА при запуске через `run_bp5()` — независимо от `TRUE_ALERTING` — в том числе при запуске этапа 5 кнопкой в админке (`core/pipeline/registry.py` вызывает `run_bp5()` напрямую). `TRUE_ALERTING=False` НЕ означает «ничего никуда не уйдёт» — уйдёт, просто на тестовый адрес/чат из `.env`, а не реальному получателю
- [x] 4.3 Указать, что полностью отключить попытку отправки (`deliver=False`, сеть не трогается вообще) можно только через сидер демоданных (`core/scripts/stages/bp5.py`) — из админки такой возможности нет, `run_bp5()` всегда вызывает `sync_alerts` с `deliver=True` по умолчанию

## 5. Правка `.env.example`

- [x] 5.1 Поправить комментарий у `TRUE_ALERTING` в `.env.example` (сейчас: «# --- Alerting (флаг реальной отправки предупреждений) ---» и «# Если стоит False то и письмо и тг будут использоваться TEST_EMAIL, TEST_TG») — вторая строка верна по сути, но заголовок «флаг реальной отправки» вводит в заблуждение (звучит как «включает/выключает отправку»); уточнить, что отправка идёт всегда, флаг выбирает только адрес

## 6. Финальная сверка

- [x] 6.1 Перечитать получившийся `BP5_README.md` целиком — сверить с `src/bp5/pipeline.py` (реальная доставка, `deliver`, `true_alerting`), `core/config.py` (имена полей `true_alerting`/`test_email`/`test_tg`) и `core/scripts/stages/dictionaries.py` (демо-данные `EVENT_TYPES`/`CHANNELS`/`USERS`/`ROUTING`) — убедиться, что нигде не осталось противоречий со старой (уже неверной) версией текста
