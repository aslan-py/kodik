## 1. Синхронизация покрытия

- [x] 1.1 Создать модуль синхронизации покрытия в `src/bp1/` с докстрингом: зачем нужна, что создаёт, чего намеренно не делает (не удаляет, не деактивирует, не создаёт задачи с поисковым словом)
- [x] 1.2 Отобрать активные источники и активных конкурентов, собрать все их пары
- [x] 1.3 Вставить задачи одной пачкой с игнорированием конфликтов — существующие ограничения `search_task` уже гасят дубли, вычитывать текущее состояние в память не требуется (см. design.md, Decisions)
- [x] 1.4 Создавать задачи только без поискового слова; наличие у пары задачи с поисковым словом покрытием не считать
- [x] 1.5 Вернуть число реально созданных задач
- [x] 1.6 Убедиться, что деактивированная задача считается существующей и повторно не создаётся, а её признак активности не меняется
  - `tests/bp1/test_search_task_coverage.py`: 3 passed — активные пары, идемпотентность, отдельное покрытие при наличии trigger-задачи и сохранение деактивированной задачи.

## 2. Встраивание в прогон сбора

- [x] 2.1 `src/bp1/pipeline.py::run_bp1()` — вызвать синхронизацию до обхода источников, в той же сессии
- [x] 2.2 Добавить число созданных задач в сводку прогона
- [x] 2.3 Обновить докстринг `run_bp1()` — порядок шагов теперь начинается с синхронизации покрытия
- [x] 2.4 Проверить взаимодействие с предусловием этапа 1 из `connect-real-bp1-parsing`: если задач нет, но есть активные источники и конкуренты, синхронизация должна отработать до проверки — иначе preflight отклонит прогон, который сам бы себе создал задачи
  - Реальный BP-1 теперь имеет `requires=None`: пустой `search_task` допустим, покрытие создаёт сам этап до обхода. Интеграционный тест проверяет порядок и одну сессию.

## 3. Отдельный запуск

- [x] 3.1 Дать возможность выполнить синхронизацию без обхода источников (отдельная команда либо аргумент существующей)
- [x] 3.2 Убедиться, что при отдельном запуске не происходит ни одного обращения к внешним источникам
- [x] 3.3 Строка в `core/scripts/SCRIPTS_README.md` — как посмотреть объём до запуска дорогого сбора
  - Команда: `python -m src.bp1.search_task_coverage`; использует только сессию БД, тест подтверждает отсутствие запуска `AdaptiveRunner`/Redis.

## 4. Админка и документация

- [x] 4.1 В разделе задач сбора явно указать, что исключение пары выполняется деактивацией: удалённая задача будет создана заново при следующей синхронизации
- [x] 4.2 Проверить, доступно ли удаление задачи сбора в интерфейсе, и рассмотреть его отключение в пользу деактивации (см. design.md, Risks)
- [x] 4.3 `src/bp1/README.md` — описать синхронизацию покрытия и её место в цепочке BP-3 → BP-7 → сбор
- [x] 4.4 `src/bp7/BP7_README.md` — дописать, что перенесённый источник начинает собираться на ближайшем прогоне сбора автоматически, ручное заведение задач больше не требуется
  - Удаление уже отключено для `SearchTaskAdmin` общим `KodikModelAdmin.has_delete_permission()`; правило деактивации добавлено в инструкцию админки.

## 5. Проверка

- [x] 5.1 Чистая база со справочниками, задач сбора нет — прогон сбора создаёт задачи по всем активным парам и выполняет обход
- [x] 5.2 Добавить источник через админку, прогнать сбор — появились задачи по нему на каждого активного конкурента
- [x] 5.3 Добавить конкурента через админку, прогнать сбор — появились задачи по нему на каждый активный источник
- [x] 5.4 Деактивировать задачу, прогнать сбор дважды — задача остаётся деактивированной, дубль не появляется, сбор по ней не идёт
- [x] 5.5 Деактивировать источник, прогнать сбор — новые задачи по нему не создаются, существующие не тронуты; включить обратно — сбор по ним возобновляется
- [x] 5.6 Пара, у которой есть только задача с поисковым словом — создаётся дополнительная задача без слова, существующая не изменена
- [x] 5.7 Прогон без изменений в справочниках — создано ноль задач, в сводке это видно
- [x] 5.8 Сквозная проверка замыкания цепочки: провести кандидата через BP-3 → BP-7 до справочника источников, затем прогнать сбор — по новому источнику появились задачи и выполнился обход, без ручных действий
- [x] 5.9 Оценить объём на демо-справочниках (`stages/bp1.py`, ~49 конкурентов) — зафиксировать число созданных задач и длительность прогона; если объём неприемлем, вынести ограничение охвата отдельной задачей (см. design.md, Risks)
  - Откатываемый замер PostgreSQL: 49 активных конкурентов × 1 источник → 49 задач за 8,62 мс; отдельное ограничение охвата не требуется.
- [x] 5.10 `ruff check` по новым и изменённым файлам, прогон `pytest`
  - `ruff check .`: passed. Целевые тесты: 10 passed. Полный `pytest`: 467 passed, 14 failed, 31 errors; остаток известный и не связан с изменением (нет fixture `mocker`, прежние BP5 state-зависимые падения и один adaptive parser test).
