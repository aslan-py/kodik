## 1. Подготовка

- [x] 1.1 Убедиться, что `connect-real-bp1-parsing` завершён и реальный сбор хотя бы раз отработал — чистим проверенный в работе код, а не предполагаемо ненужный — заархивирован, реальный сбор проверен в предыдущей сессии
- [x] 1.2 Зафиксировать базовую линию: прогнать `pytest tests/bp1/` и записать, сколько тестов проходит до чистки — **233 passed, 31 errors** (все ошибки — `fixture 'mocker' not found` в `tests/bp1/fedresurs/*`, предсуществующие, не связаны с чисткой)

## 2. Удаление MCP-сервера

- [x] 2.1 Найти по всему репозиторию строки `mcp_server`, `MCPServer`, `run_mcp_server`, `MCPTool` (включая `.md`, `.toml`, `.txt`) — составить полный список хвостов — найдено 10 файлов: сам модуль+тест, `adaptive/__init__.py` (3 места), `integration/__init__.py`, `schemas.py`, `adaptive/README.md` (5 мест), `src/bp1/README.md` (5 мест)
- [x] 2.2 Убрать реэкспорты из `src/bp1/adaptive/__init__.py` (import + записи в `__all__`) и упоминание из `adaptive/integration/__init__.py`
- [x] 2.3 Удалить `tests/bp1/adaptive/test_mcp.py`
- [x] 2.4 Удалить `src/bp1/adaptive/integration/mcp_server.py`
- [x] 2.5 Проверить `MCPTool` в `adaptive/schemas.py` — удалить, если потребителей не осталось — потребителей не было, удалён
- [x] 2.6 Прогнать `pytest tests/bp1/` — сверить с базовой линией из 1.2 (минус тесты MCP, других падений быть не должно) — **228 passed, 31 errors** (было 233; минус 5 тестов `test_mcp.py`; те же 31 предсуществующие ошибки)

## 3. Удаление неиспользуемых сборщиков

- [x] 3.1 Удалить 7 пустых заготовок: `collectors/fips_rpa`, `google_news_rpa`, `hh_api`, `vk_api`, `zakupki_rpa`, `nic_ru_rpa`, `kodik_forum_rpa`
- [x] 3.2 Удалить `collectors/kad_arbitr_rpa` целиком (вместе с `tests_site/` на 2.8 МБ и собственным `requirements.txt`) — 2.9 МБ освобождено
- [x] 3.3 Проверить, что `collectors/stealth` и `collectors/fedresurs_rpa` не задеты и `ParserFactory` по-прежнему отдаёт адаптер fedresurs — подтверждено
- [x] 3.4 Почистить документацию: `PREDPROD_README.md:92`, `src/bp1/README.md` (строка дерева, раздел `KadArbitrParser` целиком, checklist «План развития», features-список) — везде найдено больше упоминаний, чем предполагалось изначально (6 мест в `src/bp1/README.md`), почищено всё
- [x] 3.5 Прогнать `pytest tests/bp1/` — падений быть не должно — **228 passed, 31 errors** (те же предсуществующие)

## 4. Конфигурация через settings

- [x] 4.1 Добавить в `core/config.py` поля под LLM-доступы BP-1 (ключ, базовый URL, модель) — как отдельные поля, не переиспользуя `openrouter_api_key`/`deepseek_token` (см. design.md, Decisions)
- [x] 4.2 `adaptive/processing/llm.py` — заменить `os.getenv` на чтение из `settings`
- [x] 4.3 `adaptive/processing/llm.py` — удалить самописный разбор `.env` (функция, раскладывающая пары в `os.environ`) — вместе с `_PROJECT_ROOT`/`_ENV_PATH`, ставшими ненужными
- [x] 4.4 `adaptive/cli.py` — убрать собственный `load_dotenv()` и поясняющий комментарий про то, почему он был нужен
- [x] 4.5 `adaptive/llm_test.py` — заменить три `os.getenv` на `settings` (файл решено оставить — см. 5.1)
- [x] 4.6 Проверить, что в `src/bp1/` не осталось ни одного `os.getenv`/`os.environ`/`load_dotenv` (кроме осознанно оставленных с пояснением) — чисто (только устаревшие `.pyc` в кэше, не источник)
- [x] 4.7 Прогнать реальный сбор (`TRUE_PARSING=True`) — убедиться, что LLM-вызовы адаптивного парсера по-прежнему получают ключ — прогнано, ни одной ошибки/`401` в выводе

## 5. Отладочные скрипты

- [x] 5.1 Решить судьбу `src/bp1/test_parser.py` и `adaptive/llm_test.py`: перенести в `tests/`, оставить с пояснением в докстринге или удалить — выбрать и применить единообразно к обоим — решение: **оставить на месте** (`testpaths = ["tests"]` в `pyproject.toml` не собирает их, несмотря на имя `test_with_real_db` в первом)
- [x] 5.2 Если оставляем — дописать в докстринг каждого, что это ручной отладочный скрипт, а не автотест, и как его запускать

## 6. Финал

- [x] 6.1 Проверить `requirements.txt` — освободились ли пакеты, нужные только удалённому коду; убрать освободившиеся — **ничего не освободилось**: `kad_arbitr_rpa/requirements.txt` (git-история) требовал только `playwright`/`pydantic`/`aiofiles`, все уже в основном файле и используются `fedresurs_rpa`/`stealth`/остальным кодом; MCP-сервер был самописным, внешнего пакета `mcp` не тянул. Отдельно (не в рамках этой задачи) замечено: `litellm` в requirements не импортируется нигде в коде, и `crawl4ai` продублирован двумя строками — оба предсуществуют этой чистке, не связаны с удалённым кодом
- [x] 6.2 `ruff check` по всему `src/bp1/` — чисто (и по `tests/bp1/` тоже)
- [x] 6.3 Полный прогон `pytest` — сверить с базовой линией из 1.2 — **найдена и исправлена регрессия**: перевод `llm.py` на `settings` (раздел 4) сломал 5 тестов (`test_llm.py` ×3, `test_llm_smoke.py` ×1, `test_integration.py` ×1), которые патчили `os.environ` через `monkeypatch.setenv/delenv` — но код теперь читает закэшированный `settings`, патч окружения на него не действует. Исправлено: тесты (4 файла, включая `tests/bp1/adaptive/test_parser.py`, найденный тем же grep) теперь патчат `settings.llm_api_key` напрямую через `monkeypatch.setattr`; заодно удалены омертвевшие константы `ENV_LLM_API_KEY`/`ENV_OPENAI_API_KEY` и весь блок `MCP_*` в `tests/bp1/adaptive/constants.py` (упущены в разделе 2). Итог: **228 passed, 31 errors** — точно базовая линия
- [x] 6.4 Прогнать конвейер целиком (этапы 1-7) в обоих режимах `TRUE_PARSING` — убедиться, что чистка ничего не задела. По решению пользователя — только BP-1 в обоих режимах (эта задача трогает только `src/bp1/`, полный прогон 1-7 расходовал бы LLM/Tavily на не тронутых чисткой этапах). Реальный режим: `OK`, без ошибок. Заглушка: код доходит до `bp1_stub.seed()` и корректно сообщает о нехватке справочника `dictionaries` — ожидаемо при текущем состоянии БД (реальные данные пользователя вместо демо-источника), не баг чистки; пересеивать не стал, чтобы не снести реальный сетап пользователя
