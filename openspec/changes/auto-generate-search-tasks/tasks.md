## 1. Синхронизация покрытия

- [ ] 1.1 Создать модуль синхронизации покрытия в `src/bp1/` с докстрингом: зачем нужна, что создаёт, чего намеренно не делает (не удаляет, не деактивирует, не создаёт задачи с поисковым словом)
- [ ] 1.2 Отобрать активные источники и активных конкурентов, собрать все их пары
- [ ] 1.3 Вставить задачи одной пачкой с игнорированием конфликтов — существующие ограничения `search_task` уже гасят дубли, вычитывать текущее состояние в память не требуется (см. design.md, Decisions)
- [ ] 1.4 Создавать задачи только без поискового слова; наличие у пары задачи с поисковым словом покрытием не считать
- [ ] 1.5 Вернуть число реально созданных задач
- [ ] 1.6 Убедиться, что деактивированная задача считается существующей и повторно не создаётся, а её признак активности не меняется

## 2. Встраивание в прогон сбора

- [ ] 2.1 `src/bp1/pipeline.py::run_bp1()` — вызвать синхронизацию до обхода источников, в той же сессии
- [ ] 2.2 Добавить число созданных задач в сводку прогона
- [ ] 2.3 Обновить докстринг `run_bp1()` — порядок шагов теперь начинается с синхронизации покрытия
- [ ] 2.4 Проверить взаимодействие с предусловием этапа 1 из `connect-real-bp1-parsing`: если задач нет, но есть активные источники и конкуренты, синхронизация должна отработать до проверки — иначе preflight отклонит прогон, который сам бы себе создал задачи

## 3. Отдельный запуск

- [ ] 3.1 Дать возможность выполнить синхронизацию без обхода источников (отдельная команда либо аргумент существующей)
- [ ] 3.2 Убедиться, что при отдельном запуске не происходит ни одного обращения к внешним источникам
- [ ] 3.3 Строка в `core/scripts/SCRIPTS_README.md` — как посмотреть объём до запуска дорогого сбора

## 4. Админка и документация

- [ ] 4.1 В разделе задач сбора явно указать, что исключение пары выполняется деактивацией: удалённая задача будет создана заново при следующей синхронизации
- [ ] 4.2 Проверить, доступно ли удаление задачи сбора в интерфейсе, и рассмотреть его отключение в пользу деактивации (см. design.md, Risks)
- [ ] 4.3 `src/bp1/README.md` — описать синхронизацию покрытия и её место в цепочке BP-3 → BP-7 → сбор
- [ ] 4.4 `src/bp7/BP7_README.md` — дописать, что перенесённый источник начинает собираться на ближайшем прогоне сбора автоматически, ручное заведение задач больше не требуется

## 5. Проверка

- [ ] 5.1 Чистая база со справочниками, задач сбора нет — прогон сбора создаёт задачи по всем активным парам и выполняет обход
- [ ] 5.2 Добавить источник через админку, прогнать сбор — появились задачи по нему на каждого активного конкурента
- [ ] 5.3 Добавить конкурента через админку, прогнать сбор — появились задачи по нему на каждый активный источник
- [ ] 5.4 Деактивировать задачу, прогнать сбор дважды — задача остаётся деактивированной, дубль не появляется, сбор по ней не идёт
- [ ] 5.5 Деактивировать источник, прогнать сбор — новые задачи по нему не создаются, существующие не тронуты; включить обратно — сбор по ним возобновляется
- [ ] 5.6 Пара, у которой есть только задача с поисковым словом — создаётся дополнительная задача без слова, существующая не изменена
- [ ] 5.7 Прогон без изменений в справочниках — создано ноль задач, в сводке это видно
- [ ] 5.8 Сквозная проверка замыкания цепочки: провести кандидата через BP-3 → BP-7 до справочника источников, затем прогнать сбор — по новому источнику появились задачи и выполнился обход, без ручных действий
- [ ] 5.9 Оценить объём на демо-справочниках (`stages/bp1.py`, ~49 конкурентов) — зафиксировать число созданных задач и длительность прогона; если объём неприемлем, вынести ограничение охвата отдельной задачей (см. design.md, Risks)
- [ ] 5.10 `ruff check` по новым и изменённым файлам, прогон `pytest`
