## 1. Агрегация причин отсева

- [x] 1.1 В `src/bp2/pipeline.py` посчитать разбивку по `reject_reason` среди строк, собранных `build_normalized_rows()` (до `upsert_normalized_items`) — по одному счётчику на `black_domain`, `stop_word`, `stop_topic`, `false_positive`, `parse_error`, с нулями там, где причина не сработала ни разу
- [x] 1.2 Добавить разбивку в dict, который возвращает `run_bp2()`, отдельным полем (не смешивая с `noise_rejected`)
- [x] 1.3 Обновить docstring `run_bp2()` — упомянуть новое поле сводки

## 2. Тесты

- [x] 2.1 Добавить тест на `run_bp2()` (или на функцию агрегации, если она вынесена отдельно от `build_normalized_rows`), который прогоняет строки с разными причинами отсева и проверяет, что каждая причина посчитана верно
- [x] 2.2 Добавить тест на случай без отклонений — все причины возвращаются нулями, а не отсутствуют в структуре
- [x] 2.3 Проверить, что `noise_rejected` остаётся отдельным полем и не задваивается с разбивкой по причинам в прогоне, где сработали оба вида отсева

## 3. Проверка вручную

- [x] 3.1 Прогнать этап 2 через `core/pipeline/cli.py` на данных с известными чёрным доменом/стоп-словом — сверить разбивку в выводе с ожидаемой
