## 1. Ревизия скриптов

- [x] 1.1 Составить полный список исполняемых модулей `core/scripts/` (включая `stages/`) с отметкой «описан в README / нет»
  - 15 точек запуска: `clear_data`, `seed_all`, `seed_data`, `universal_script_1`; этапы `bp1`, `bp1_stub`, `bp2`…`bp7`, `dictionaries`, `simple_dictionaries`; удалённый ниже `migrate_raw_files`. До ревизии README не упоминал `seed_data` и `migrate_raw_files`; остальные 13 команд были указаны. `stages/cascade.py` — библиотечный helper без `__main__`, точкой запуска не является.
- [x] 1.2 Прогнать `seed_data.py` на чистой базе — проверить, что он работает с текущей схемой БД и заливает `core/data/*.csv` (источники, конкуренты, задачи сбора)
  - В отдельной временной PostgreSQL-базе после `alembic upgrade head`: 4 конкурента, 9 источников, 5 триггеров, 56 задач. База после проверки удалена.
- [x] 1.3 Проверить `migrate_raw_files.py`: совпадает ли формат, к которому он приводит, с тем, что создаётся сейчас — от этого зависит решение об удалении
  - Целевые поля мигратора (`published_at`, `media_name`, `extra`) уже являются текущим контрактом `ParsedItem`. На актуальном JSON повторный прогон потерял бы значения: скрипт читает только старые `date`, `media`, `salary`. Каталог `data/raw` не содержит старых JSON для миграции.
- [x] 1.4 Оценить `universal_script_1.py`: покрывается ли его сценарий комбинацией других команд после переписывания README (см. design.md, Decisions)
  - Оставлен как полезная интерактивная команда «зависимости → миграции → детерминированное демо BP1–BP5 → API». README также даёт прозрачный неинтерактивный эквивалент отдельными командами.

## 2. Переписать SCRIPTS_README

- [x] 2.1 Раздел «Развернуть с нуля» — порядок команд от пустой базы до рабочего состояния
- [x] 2.2 Раздел «Тестовый набор для реального сбора» — `seed_data.py` и `core/data/*.csv`: какие источники, конкуренты и задачи сбора заливаются и чем этот набор отличается от демо-данных `scripts_data/`
- [x] 2.3 Раздел «Прогнать конвейер на тестовых данных» — `simple_dictionaries` + `bp1_stub`, что проверяется и почему это дёшево
- [x] 2.4 Раздел «Наполнить витрину демо-данными» — `dictionaries` + `stages/bp1` и далее по этапам
- [x] 2.5 Раздел «Очистка» — `clear_data.py` с обоими режимами, и чем каскадная очистка отличается от очистки одного слоя
- [x] 2.6 Явно развести три способа наполнить первый этап (реальный сбор / демо-датасет / заглушка): что даёт, чего стоит, когда брать
- [x] 2.7 Оставить сводную таблицу «команда → что заполняет» справочником в конце, дополнив недостающими строками
- [x] 2.8 Записать в README правило: новый исполняемый модуль в `core/scripts/` либо описывается здесь, либо не добавляется

## 3. Привести скрипты в соответствие

- [x] 3.1 Удалить `migrate_raw_files.py`, если проверка 1.3 подтвердила, что он неприменим
- [x] 3.2 Применить решение по `universal_script_1.py` из 1.4: удалить либо обновить докстринг и описание в README под текущий состав конвейера (BP-3 больше не заглушка)
- [x] 3.3 Дописать в докстринг `seed_data.py` его назначение и связь с `core/data/*.csv` — сейчас связь видна только из кода
- [x] 3.4 Проверить докстринги остальных остающихся скриптов на устаревшие утверждения о составе конвейера
  - Убрано устаревшее пояснение `cascade.count_rows` про preflight BP1; остальные упоминания заглушек относятся к реально существующим демо-сидерам.

## 4. Проверка

- [x] 4.1 Пройти сценарий «развернуть с нуля» по README буквально, с чистой базой — команды выполняются в описанном порядке и приводят к рабочему состоянию
  - На чистой временной базе миграции + `seed_all` завершились: 106 строк витрины и 63 action item; приложение импортируется, зарегистрировано 6 маршрутов. Найден и исправлен конфликт с admin, которого создаёт миграция: демо-пользователи теперь добавляются рядом с ним.
- [x] 4.2 Пройти сценарий «прогнать конвейер на тестовых данных» — приводит к ожидаемым 2 строкам `status=ok` после BP-2
  - `simple_dictionaries` → `bp1_stub` → настоящий `core.pipeline.cli 2`: 6 строк, из них 2 `ok`, 4 `rejected`.
- [x] 4.3 Пройти сценарий «тестовый набор для реального сбора» — после него в БД есть источники, конкуренты и задачи сбора, достаточные для запуска реального сбора
  - Подтверждено изолированным запуском `seed_data`: 4 активных конкурента, 9 источников, 56 исходных задач; BP1 дополнительно синхронизирует triggerless-покрытие при старте.
- [x] 4.4 Сверить: каждый исполняемый модуль `core/scripts/` присутствует в README, и каждая команда из README существует
  - 14/14 точек запуска присутствуют; все 17 уникальных модулей из команд README разрешаются через Python import machinery.
