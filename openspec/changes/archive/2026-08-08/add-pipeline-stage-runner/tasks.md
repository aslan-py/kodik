## 1. Этап 3: реальная точка входа

- [x] 1.1 Создать `src/bp3/pipeline.py::run_bp3() -> dict` — извлечь сборку `Pipeline([...])` из корневого `main.py` (те же 9 модулей, тот же порядок), вернуть сводку по прогону вместо `print()`
- [x] 1.2 Не менять и не удалять `main.py` — оставить как есть для ручной отладки коллегой

## 2. Этап 7: заглушка наполнения `source_candidate`

- [x] 2.1 Создать `core/scripts/stages/bp7.py` с парой `clear(session)`/`seed(session)`, по образцу `bp1.py`/`bp6.py`
- [x] 2.2 `seed()` заполняет `source_candidate` несколькими демо-строками через модель `src/bp7/models.py::SourceCandidate` — часть со `score` выше `SOURCE_CANDIDATE_SCORE_THRESHOLD`, часть ниже, `status=new`
- [x] 2.3 `clear()` — по аналогии с существующими сидерами (снос своего слоя, без каскада ниже — `source_candidate` листовой)
- [x] 2.4 `SourceCandidate` уже в `DICTIONARY_ORDER` (core/scripts/stages/cascade.py) — проверено, ничего добавлять не пришлось

## 3. Реестр этапов

- [x] 3.1 Создать `core/pipeline/__init__.py`
- [x] 3.2 Создать `core/pipeline/registry.py` с типом дескриптора этапа (номер, заголовок, `is_stub`, функция preflight или `None`, функция запуска) и картой `STAGES: dict[int, StageDescriptor]` на все 7 этапов
- [x] 3.3 Подключить в `STAGES` реальные функции без изменений: `run_bp2`, `run_bp3` (из п.1.1), `run_bp4`, `run_bp5`, `run_bp7_promotion`
- [x] 3.4 Подключить в `STAGES` этапы 1 и 6 как заглушки (`is_stub=True`): обёртки над `clear()+seed()` из `core/scripts/stages/bp1.py` и `bp6.py` (не голый `seed()` — см. design.md, Decisions)
- [x] 3.5 Этап 7 собран как ОДИН составной вызов (`_run_bp7_stage`): сначала `clear()+seed()` из `core/scripts/stages/bp7.py` (заглушка наполнения), потом `run_bp7_promotion()` (реальный перенос) — не два отдельных номера этапа, а последовательный вызов внутри одного, чтобы сохранить соответствие «7 этапов = 7 BP» (решение принято по ходу реализации, не меняет спеку/tasks по сути)

## 4. Preflight-проверки

- [x] 4.1 Функция preflight для этапов 2–6: через `count_rows()` из `cascade.py` проверить, что в таблице слоя N−1 (по `PIPELINE_ORDER`) есть хотя бы одна строка; иначе — ошибка с указанием, каких данных не хватает и какой этап прогнать раньше
- [x] 4.2 Первый этап конвейера — без preflight (входная точка)
- [x] 4.3 Этап 7: `requires=None` — предусловий нет, пустая очередь `source_candidate` не может быть ошибкой, так как её и не проверяют (эквивалентно «отдельной preflight-функции, которая никогда не падает»)

## 5. Раннер и CLI

- [x] 5.1 `core/pipeline/runner.py::run_stage(n)` — preflight → вызов функции этапа → результат; понятная ошибка на несуществующем номере этапа (1–7)
- [x] 5.2 `core/pipeline/runner.py::run_all()` — прогон этапов 1→7 по порядку, каждый в своём `try/except`, сбой одного не прерывает цикл
- [x] 5.3 `run_all()` возвращает сводку по всем 7 этапам: успех / ошибка (с текстом) / не запущен, включая пометку `is_stub` там, где применимо
- [x] 5.4 `core/pipeline/cli.py` — минимальный CLI: запуск одного этапа по номеру, запуск всего конвейера (без лишних флагов/подкоманд)

## 6. Проверка вручную

- [x] 6.1 Прогнать каждый этап 1–7 по отдельности на пустой БД — этап 2 (и последующие) должны явно упасть на preflight, пока не прогнан этап 1
- [x] 6.2 Прогнать `run_all()` на пустой БД от начала до конца — все 7 этапов успешны, сводка показывает заглушки на 1/6/7-заполнении
- [x] 6.3 Специально уронить один этап (подмена `STAGES[3].run` на исключение в одноразовом процессе — файлы не менялись) и прогнать `run_all()` — подтверждено: остальные этапы выполняются, сводка показывает первопричину на этапе 3 и каскадные «нет данных» на 4/5/6 отдельно друг от друга, независимый этап 7 не затронут
