## 1. Выбрать конкретные данные

- [x] 1.1 Выбрать одного конкурента и два `id` новостей из `core/scripts/stages/news_data.py::NEWS` с `is_clean == True` (проходят фильтры BP-2 без отсева) — выбрано: «АвтоМЛ», id 42 и 58

## 2. Минимальный сидер

- [x] 2.1 Создать `core/scripts/stages/bp1_stub.py` (имя уточнить, не путать с `bp1.py`) с парой `clear(session)`/`seed(session)`, по образцу существующих стабов (`bp6.py`/`bp7.py`) — не переиспользовать сложную логику `bp1.py` (грязный текст, тай-брейки, сценарии фильтров — тут не нужны)
- [x] 2.2 `seed()` — одна `SearchTask` на выбранного конкурента, один `RawItem` с двумя выбранными новостями (чистый текст, без искажений)
- [x] 2.3 `clear()` — снос своего слоя по аналогии с `bp1.py::clear()` (`clear_from(session, SearchTask)`)

## 3. Переключить реестр пайплайна

- [x] 3.1 В `core/pipeline/registry.py` — заглушка этапа 1 (`STAGES[1]`) указывает на `bp1_stub.clear`/`bp1_stub.seed` вместо `bp1_stage` (`core/scripts/stages/bp1`)
- [x] 3.2 Проверить, что импорт `bp1_stage` из `core/scripts/stages/bp1` больше не нужен в `registry.py` (или оставить, если используется где-то ещё — сверить)

## 4. Проверка вручную

- [~] 4.1 Прогнать этап 1 через `core/pipeline/cli.py`/админку на пустой БД — убедиться, что появился ровно 1 `raw_item` — **пропущено сознательно**: пользователь попросил не тратить время на ручное тестирование BP-1, это всегда заглушка
- [~] 4.2 Прогнать этап 2 следом — убедиться, что в `normalized_item` ровно 2 строки со `status=ok` — **пропущено** (см. 4.1)
- [~] 4.3 Убедиться, что `python -m core.scripts.stages.bp1` (полный демо-сидер) по-прежнему работает как раньше и не зависит от нового стаба — **пропущено** (см. 4.1); код `bp1.py` не тронут этим change, риска нет
