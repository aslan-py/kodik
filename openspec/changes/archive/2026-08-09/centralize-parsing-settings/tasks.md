## 1. Инвентаризация и разметка

- [x] 1.1 Составить полный список констант-кандидатов из `src/bp1/constants.py`, `adaptive/processing/parser.py`, `adaptive/core/cache.py`, `adaptive/strategies/orchestrator.py` и дефолтов `AdaptiveRunner.__init__`
- [x] 1.2 По каждой отметить «выносим / оставляем в коде» по критерию из design.md; зафиксировать текущее значение как дефолт. **Выносим (14):** `DEFAULT_MAX_NEWS`, `MIN_ARTICLE_TEXT_LENGTH`, `MIN_FULL_ARTICLE_TEXT_LENGTH`, `MAX_TAIL_FETCH_ATTEMPTS`, `MIN_CONTENT_LENGTH` (объём/глубина); `DEFAULT_TIMEOUT_MS`+`AdaptiveRunner.timeout` (одно поле на оба — тот же смысл, тот же дефолт 60000), `ARTICLE_FETCH_TIMEOUT_SECONDS`, `MAX_CONCURRENT_FETCHES`, `DEFAULT_MAX_CONCURRENT`+`AdaptiveRunner.max_concurrent` (аналогично, один сеттинг на оба) (сеть); `ADAPTER_TTL_SECONDS`, `CLASSIFICATION_TTL_SECONDS`, `ARTICLE_TEXT_TTL_SECONDS` (кэши); `AdaptiveRunner.mode`, `AdaptiveRunner.headless` (режим прогона). **Оставляем в коде:** `TAIL_FETCH_CHUNK_SIZE` (деталь чанкера), `LOG_SEPARATOR_WIDTH`/`CLI_SEPARATOR_WIDTH` (оформление), `STATUS_*`/`MODE_*`/`SEPARATOR_LINE` (доменные литералы), `quality_gate_enabled`/`cache_profiles` (не входят в явный список task 4.2 — оставлены `True` как есть)
- [x] 1.3 Проверить `FLOWER_PORT`: запускается ли Flower в проекте — не упоминается нигде, кроме `.env` и `requirements.txt` (сам пакет), в `docker-compose.yml` сервиса нет → решение: удалить

## 2. Настройки объёма и глубины сбора

- [x] 2.1 Добавить в `core/config.py` поля: сколько новостей брать с источника, минимальная длина текста статьи, минимальная длина полной статьи, число попыток дочитывания
- [x] 2.2 Добавить поле минимальной длины содержимого для оркестратора стратегий (`MIN_CONTENT_LENGTH`)
- [x] 2.3 Переопределить константы в модулях через `settings` — имена констант сохранить, места использования не трогать (см. design.md, Decisions)
- [x] 2.4 Сверить «было/стало»: значения по умолчанию совпадают с прежними константами — проверено программно, все 7 значений совпали

## 3. Настройки сетевых ограничений

- [x] 3.1 Добавить поля: таймаут запроса статьи, таймаут парсинга, число параллельных запросов статей, число параллельных задач сбора
- [x] 3.2 Заменить `DEFAULT_TIMEOUT_MS`/`DEFAULT_MAX_CONCURRENT` в `src/bp1/constants.py` на чтение из `settings`
- [x] 3.3 Сверить «было/стало» — совпадает (60000, 5)

## 4. Настройки кэшей и режима прогона

- [x] 4.1 Добавить три отдельных поля TTL: профиль адаптера, классификация источника, текст статьи (не одно общее — см. design.md)
- [x] 4.2 Добавить поля режима прогона: режим `AdaptiveRunner` (`adaptive`/`hybrid`/`fallback`) и headless-режим браузера
- [x] 4.3 `src/bp1/pipeline.py::run_bp1()` — передавать режим и параметры из `settings` вместо дефолтов конструктора (снимает Open Question из `connect-real-bp1-parsing`) — теперь явно передаёт `mode`, `headless`, `timeout`, `max_concurrent` при создании `AdaptiveRunner`
- [x] 4.4 Сверить «было/стало» — совпадает (604800×3, `adaptive`, `True`)

## 5. Чистка .env

- [x] 5.1 Удалить `KODIK_API_KEY` из `.env` и `.env.example` — не читается нигде
- [x] 5.2 Свести LLM-доступы: убрать `OPENAI_API_KEY` (оставить `LLM_API_KEY`), убрать `DEEPSEEK_API_KEY` (оставить `DEEPSEEK_TOKEN`); убрать fallback-чтение старых имён из кода — код уже не читал их (убрано в `cleanup-bp1-dead-code`), проверено grep'ом — чисто
- [x] 5.3 Применить решение по `FLOWER_PORT` из 1.3 — удалён
- [x] 5.4 Проверить: каждая оставшаяся переменная `.env` либо является полем `Settings`, либо лежит в отдельной секции «только для docker-compose» с явным заголовком — проверено программно (сверка `.env` × `Settings`), расхождений нет

## 6. Порядок в .env и .env.example

- [x] 6.1 Сгруппировать переменные по назначению с заголовками-комментариями, в том же порядке, что блоки в `Settings`
- [x] 6.2 Дать каждой переменной короткое пояснение: зачем нужна и кто читает
- [x] 6.3 Переписать `.env.example` как документацию: обязательные — пустыми с пометкой, необязательные — со значениями по умолчанию — все 14 обязательных полей (без дефолта в `Settings`) отмечены, совпадение точное
- [x] 6.4 Сверить состав: в `.env.example` есть всё, что читает `Settings`, и нет ничего лишнего — проверено программно в обе стороны (после добавления забытого `REDIS_DB`), расхождений нет

## 7. Проверка

- [x] 7.1 Запуск приложения с `.env`, скопированным из `.env.example` и заполненным только обязательными переменными — стартует штатно — проверено через `Settings(_env_file=...)` на изолированном временном файле (живой `.env` не трогал), все необязательные поля взяли дефолты
- [x] 7.2 Запуск с незаполненной обязательной переменной — падает на старте с понятным указанием, какой переменной не хватает — `pydantic.ValidationError: jwt_secret_key Field required`
- [x] 7.3 Прогнать реальный сбор (`TRUE_PARSING=True`) — результат совпадает с тем, что было до выноса констант — `OK`, `{'saved': 0, 'unchanged': 1, 'error': 0}`, без ошибок
- [x] 7.4 Изменить один параметр в `.env` (например, объём собираемого), перезапустить, убедиться, что поведение изменилось соответственно — `BP1_MAX_NEWS_PER_SOURCE=1` → `parser.DEFAULT_MAX_NEWS` стал `1`; после отката строки — снова дефолтные `5`
- [x] 7.5 Проверить, что в `src/bp1/` не осталось прямого чтения окружения — чисто
- [x] 7.6 `ruff check` по изменённым файлам, полный прогон `pytest` — чисто; **228 passed, 31 errors** (те же предсуществующие)
