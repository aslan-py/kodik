"""
Слой обработки HTML и извлечения данных.

Содержит конвейер очистки → чанкирования → LLM-анализа → объединения
результатов и интеллектуальный парсер:
- ``HtmlCleaner`` — очистка HTML от шума.
- ``StructuredChunker`` / ``Chunk`` — разбиение на логические блоки.
- ``ResultMerger`` — объединение результатов чанков.
- ``LLMClient`` / ``AIAgent`` — анализ структуры через LLM.
- ``AdaptiveParser`` — интеллектуальный парсинг.
"""
