"""Поэтапный сидинг: по одному модулю на бизнес-процесс.

Каждый модуль bpN отвечает ровно за свой слой данных и устроен одинаково:

    async def seed(session)  -> int   # залить свой слой
    async def clear(session) -> int   # снести свой слой и всё ниже по потоку

Любой этап запускается сам по себе (clear + seed в одной транзакции):

    python -m core.scripts.stages.bp3

Типовой сценарий работы с одним БП — залить всё до него и дальше руками:

    dictionaries → bp1 → run_bp2 → bp3 → bp4

Общие точки входа лежат уровнем выше: core.scripts.seed_all (залить всё)
и core.scripts.clear_data (снести всё).
"""
